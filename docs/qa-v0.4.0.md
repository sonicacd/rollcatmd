# v0.4.0 用户路径 QA 清单

此文件用于记录实际人工或自动化运行结果。未勾选项目表示待验证，不代表功能失败。每次验证记录平台、应用版本、日期和使用的测试文档；涉及恢复、权限和图片写入时使用可丢弃的测试文件。

以下结果记录于 2026-09-07。`自动化`、`浏览器 UI`、`Windows 原生 UI` 和 `Android 构建/签名` 分别标注验证范围。手机宽度的浏览器布局检查不计作 Android 真机验证；最终 Windows 与 Android 包均已按定稿源码重新构建。

## 环境与构建

- [x] 自动化：`npm test` 当前 183 项全部通过；`cargo test --manifest-path src-tauri/Cargo.toml` 当前 10 项全部通过；`npm run build:renderer` 的 Vite 生产构建通过，最新一次耗时 3.25 秒。
- [x] Windows 原生 UI：初版 0.4.0 可启动，并通过命令行打开 12,626 字符、35,534 字节的测试文档。
- [x] Android 构建/签名：最终 0.4.0 已完成 Rust Release、Gradle Release 和签名检查，`versionCode=4000`、ARM64，签名证书与既有版本一致，47 项源码/前端输入哈希匹配。
- [x] 最终修复后的 Windows EXE 与 Android APK 重新构建；Windows EXE 版本 0.4.0，启动、文件名、默认大纲、阅读、本地图片回归通过。Android 无设备，未做真机启动。
- [ ] Windows 发布 EXE 显示 0.4.0，双击启动，系统默认打开和拖入文件正常。
- [ ] Android ARM64 APK 构建、安装、首次启动和系统文件选择器打开正常。
- [x] EXE、APK 和两份 SHA-256 文件上传 GitHub 后下载核对，四项哈希与本地完全一致，GitHub 资产 digest 一致。版本和大小见发布说明。

## 工具栏与阅读设置

- [x] 浏览器 UI：检查 320 / 390 / 1280 / 2400px 视口下的布局和三种主题，偏好在页面刷新后保留；宽屏下 2100px 正文实际宽度已验证。
- [ ] Windows 常规宽度、超宽窗口与 Android 竖屏均能找到新建、打开、保存、查找、大纲和更多。
- [ ] 三个主题下按钮、焦点、菜单、对话框、状态信息和编辑区文字清晰。
- [ ] 默认 16px 字号、1.5 行高、1200px 正文宽度；重启后各偏好保持。
- [ ] 14 / 16 / 18 / 20 / 24px、1.5 / 1.7 / 2、1200 / 1600 / 2100px / 全宽均生效，窄窗口不产生正文布局溢出。
- [ ] tokens 默认隐藏；启用后与字符数同屏显示，关闭后恢复，偏好可保留。
- [ ] 顶部收起/展开与移动端沉浸阅读正常，界面控件可再次找到。

## 大纲、查找与位置

- [x] 浏览器 UI：首次使用大纲默认启用并展开；关闭后刷新保持，重新启用和章节跳转通过。展开/收起与启用/停用的独立保留行为另有自动化覆盖。
- [x] Windows 原生 UI：`Ctrl+F` 在测试文档中找到一处匹配；`Ctrl+1/2/3` 三种模式切换通过。
- [x] 浏览器 UI：`Ctrl+O` 打开文件选择器后取消，374 字符原文完全保持；已复验 Toast UI `Mod-o` 列表快捷键冲突的修复。
- [ ] 标题层级、重复标题、中文标题、无标题文档和代码块内伪标题处理正确。
- [ ] 三种模式中点击大纲定位到选中章节；当前位置提示与滚动同步。
- [ ] 查找有明确工具栏入口；搜索、替换、跨行替换和关闭后继续输入正常。
- [ ] 同一次打开切换三种模式时，当前段落、文首和文末的位置恢复合理。
- [ ] 最近记录保留 20 篇，以最近使用排序，重开恢复每篇文档的模式和位置。
- [ ] 文件移动、删除、授权失效时有可操作提示；清除最近列表保留源文件和草稿。

## 恢复与保存

- [x] 浏览器 UI：使用含表格和代码块的 158 字符未命名文档，新建时取消保持内容，放弃后新建，再刷新页面可从草稿恢复完整正文。
- [x] 自动化：草稿写入/删除顺序、同 ID 恢复后继续编辑、同名记录精确删除、模式/位置更新、保存弹窗操作锁及失败重试均通过行为测试。
- [ ] 新建未命名文档，输入后停顿约 1.5 秒，恢复列表出现完整草稿。
- [ ] 持续编辑超过 10 秒，草稿能更新；包含中文、空正文、长行和较大正文的内容完整。
- [ ] 已保存文件修改后生成恢复草稿，磁盘原文件仍为上次手动保存内容。
- [ ] 强制结束测试进程，重启后能恢复已写入的未保存正文，并保持合适的模式和位置。
- [ ] 未保存文档新建、打开、拖入文件、关闭窗口时显示三选项；取消后保留当前内容和焦点。
- [ ] 选择保存并继续，成功保存后完成原操作；取消保存位置或保存失败时留在原文档。
- [ ] 保存过程中继续输入，新输入保持未保存状态，对应恢复内容保留。
- [ ] 放弃更改后原文件内容保持，恢复列表仍能找回已保留副本。
- [ ] 完整保存成功后仅移除对应草稿；其他草稿保留。
- [ ] 产生六份不同草稿后仅保留最近五份，同一草稿编辑更新不会占多个名额。
- [ ] 删除单份草稿与清除最近记录互不影响；关闭并重开应用后结果保持。
- [ ] 模拟 IndexedDB 不可用、配额不足或写入失败，界面提示且手动保存仍可完成。

## 本地图片与附件

- [x] Windows 原生 UI：测试文档中的相对本地猫图片在所见即所得与阅读视图中均可显示。
- [x] Windows 原生 UI，第三预览：在源码模式粘贴系统剪贴板 PNG，生成 `assets/image-1788795569593-37252-0.png`（20,325 字节），正文插入 `![图片](assets/...)`，右侧源码预览显示图片。`Ctrl+S` 后窗口未保存标记消失，磁盘 `native-v040.md` 尾部相对路径核对一致，保存后文件为 35,610 字节。
- [ ] Windows 打开文档旁或子目录中的 PNG/JPEG/GIF/WebP，在所见即所得与阅读中显示，并出现在全文导出结果。
- [ ] 未命名文档粘贴截图时先完成保存；取消保存不会插入无效链接或产生失控附件。
- [ ] 粘贴截图与插入本地图片写入 `assets/`，名称唯一，Markdown 使用相对路径；撤销文档编辑行为合理。
- [ ] Windows 中文路径、空格、百分号、同名附件、重复引用和丢失图片行为明确。
- [ ] 32 MiB 图片上限、SVG、绝对路径、父目录越级和目录外符号链接得到一致处理。
- [ ] 另存到不同目录后，验证附件迁移或界面提示与文档说明一致。
- [ ] Android 授权直接包含当前 Markdown 的目录，读取相对图片并在 `assets/` 写入附件。
- [ ] Android 重启后保留目录权限时仍可用；权限被撤销或来源不支持目录时有重新关联路径。

## 分享与排版

- [x] 浏览器 UI：阅读与所见即所得中的代码块复制均通过；所见即所得复制后的剪贴板文本与代码内容精确一致。
- [x] Windows 原生 UI：测试文档的 callout 显示样式正常。
- [x] 浏览器 UI：选区 PNG 剪贴板实际读回 `image/png` 并目视检查通过。所见即所得选中中文标题生成 1440px 宽图片；源码选中 374 字符（含长代码和宽表格），末尾标记 `XYZ` 与表格末尾 `END` 完整可见。两层离屏容器造成的空白问题修复已复验。
- [x] 浏览器 UI：阅读模式选区 PNG 也已实际读取剪贴板并目视验证，图片只包含所选标题；跨入后续 `pre` 元素造成的空黑框已移除并复验。
- [ ] 图片剪贴板不可用时的保存回退仍待用户路径验证。
- [ ] 三种模式选择普通文本、跨段文字、中文与格式化内容，复制 PNG 的范围与用户选择一致。
- [ ] 图片剪贴板成功时能粘贴到外部应用；不支持或权限失败时提示/保存回退与实现一致。
- [ ] 代码块复制保留代码文本与换行，复制按钮自身不会混入文本或导出图片。
- [ ] note、warning 等 callout 的所见即所得/阅读样式一致，切换模式或编辑后 Markdown 内容保持。
- [ ] 全文单页 PNG、多页 ZIP、远程图与本地图混排可导出；最终写入前取消不生成或覆盖目标文件。

## 大文档回归

- [x] Windows 原生 UI，第二预览 0.4.0：`large-v040.md` canonical 路径文件名显示正确。测试文档为 3,370,744 字节 / 1,125,924 字符 / 1,603 行，默认大纲展开，本地猫图片显示正常。
- [x] Windows 原生 UI，同一大文档：`Ctrl+G` 跳到第 800 行后切换阅读成功；关闭窗口后进程退出，重启打开同一测试文档可恢复阅读模式与中段滚动位置。
- [ ] 小于和达到 2.5 MiB 的文档分别进入正常编辑与分块渲染，普通文档编辑越过阈值时保持当前编辑器。
- [ ] 大文档中大纲、查找、模式切换、最近位置、字号/宽度设置与恢复草稿可用。
- [ ] 大文档打开、滚动和导出维持可视区分块策略，未为新增功能创建全文 DOM。
- [ ] 大文档完整草稿恢复后核对正文长度、首尾内容、随机中段及 BOM/换行设置。
- [ ] 保存/另存为、撤销/重做、窗口关闭和异常恢复没有丢失后续编辑。

## 结果记录

| 日期 | 验证类型与版本 | 检查路径 | 结果 / 证据 / 未覆盖原因 |
| --- | --- | --- | --- |
| 2026-09-07 | 自动化，0.4.0 开发工作树 | 前端测试 | `npm test`，最新 183 项通过。 |
| 2026-09-07 | 自动化，0.4.0 开发工作树 | Rust 测试 | `cargo test --manifest-path src-tauri/Cargo.toml`，10 项通过。 |
| 2026-09-07 | 构建，0.4.0 开发工作树 | Vite 生产构建 | `npm run build:renderer` 通过，最新一次耗时 3.25 秒。最终发布产物仍需最后一轮重构建。 |
| 2026-09-07 | Windows 原生 UI，初版 0.4.0 | 启动、命令行打开、本地图片、callout、查找和三模式切换 | 打开 12,626 字符 / 35,534 字节测试文档；相对猫图片在所见即所得/阅读中显示，callout 样式正常，查找一处匹配，模式快捷键通过。未据此确认拖入、系统默认打开或最终修复后的 EXE。 |
| 2026-09-07 | 浏览器 UI，0.4.0 开发页面 | 响应式布局、主题、阅读偏好、大纲 | 320 / 390 / 1280 / 2400px 视口、三主题、偏好刷新、大纲默认展开/关闭重开/章节跳转通过；已核对 2100px 正文实宽。手机宽度检查未使用 Android 真机。 |
| 2026-09-07 | 浏览器 UI，0.4.0 开发页面 | 未命名文档草稿恢复 | 158 字符文档含表格和代码块；新建取消保留、放弃新建、页面刷新后恢复完整正文通过。原生进程强制退出、持续编辑 10 秒以及大文档恢复尚待用户路径验证。 |
| 2026-09-07 | 浏览器 UI，0.4.0 开发页面 | 代码块复制 | 阅读与所见即所得均通过；所见即所得剪贴板正文已精确核对。 |
| 2026-09-07 | 浏览器 UI，0.4.0 开发页面 | 选区 PNG | 实际读取剪贴板 `image/png` 并目视验证：所见即所得选中中文标题为 1440px 宽；源码 374 字符选区含长代码/宽表格，末尾 `XYZ` 和表格末尾 `END` 可见；阅读模式图片仅包含选中标题，后续 `pre` 产生的空黑框已移除。覆盖三种模式的这些具体选区，其他选区组合、外部应用粘贴和保存回退尚待验证。 |
| 2026-09-07 | 浏览器 UI，0.4.0 开发页面 | 取消打开文件 | `Ctrl+O` 后在未保存更改确认框中取消，374 字符正文完全不变；Toast UI `Mod-o` 列表快捷键冲突修复通过复验。 |
| 2026-09-07 | Windows 原生 UI，第二预览 0.4.0 | 大文档标题、图片、跳转与重启续读 | `large-v040.md` 为 3,370,744 字节 / 1,125,924 字符 / 1,603 行，canonical 路径文件名正确、大纲默认展开、本地猫图片正常；跳到第 800 行并切阅读成功。关闭窗口后进程退出，重启同一文档恢复阅读模式和中段滚动位置。完整大文档恢复草稿、导出与其他回归项尚待验证。 |
| 2026-09-07 | Windows 原生 UI，第三预览 0.4.0 | 源码模式粘贴 PNG 与手动保存 | 从系统剪贴板粘贴 PNG 后生成 `assets/image-1788795569593-37252-0.png`（20,325 字节），插入相对图片语法并在右侧源码预览显示。`Ctrl+S` 后窗口 `*` 消失，磁盘 `native-v040.md` 尾部路径与正文一致，文件为 35,610 字节。未据此确认未命名文档保存取消、其他图片格式或其他模式的粘贴路径。 |
| 2026-09-07 | Android 构建/签名，最终 0.4.0 | Release APK 编译与签名 | ARM64、版本 0.4.0、`versionCode=4000`、同证书及 47 项输入哈希检查通过。没有连接设备，安装、运行和 SAF 用户路径未验证。 |
| 2026-09-07 | 最终 Windows EXE / Android APK | 最终构建、版本与 SHA-256 | Windows 构建 2 分 52 秒，最终 EXE 6,612,992 字节且启动回归通过；APK 10,995,019 字节。两项 SHA-256 见 [发布说明](releases/v0.4.0.md)。 |
| 本轮未覆盖 | 大文档与图片导出 | 完整草稿异常恢复、全文 PNG/ZIP 原生交互、剪贴板保存回退与其他选区组合 | 已完成的大文档原生跳转/重启续读及三模式选区 PNG 按上文范围记录。 |
| 2026-09-07 | GitHub 上传产物 | 下载地址、文件大小与 SHA-256 | 重新下载 EXE、APK 和两份校验文件，四项 SHA-256 全部与本地一致；GitHub 资产 digest 一致。 |
