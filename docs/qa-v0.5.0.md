# v0.5.0 用户路径 QA 清单

本清单用于记录最终集成工作树的实际验证。未勾选项目表示待验证；记录时请注明日期、平台、版本、测试文档及覆盖范围。浏览器窄屏检查与 Android 设备检查分别记录。文件写入、权限和失败恢复验证使用可丢弃的测试文件。

2026-09-08 验证记录：最终 v0.5.0 工作树的 JavaScript 测试 229/229、Rust 测试 11/11、生产前端构建通过；Windows EXE 和 Android 签名 APK 已产出。下列浏览器结果来自隔离的 headless Edge，浏览器文件选择和写入使用内存文件句柄，HTTPS 图片由测试路由提供。真实剪贴板脚本通过网页选区及键盘 Ctrl+C / Ctrl+V 传递实际 `text/html`；其余剪贴板事件由测试构造。原生 Markdown 转换集成使用模拟 Tauri IPC。Windows 原生界面、系统文件对话框和 Android 真机路径仍待验证。

## 环境与构建

- [x] `npm test` 完整通过：229/229。
- [x] `cargo test --manifest-path src-tauri/Cargo.toml` 完整通过：11/11。
- [x] `npm run build:renderer` 通过，生产前端构建成功。
- [x] Windows EXE 0.5.0 构建成功，产物大小 6,629,888 bytes。
- [ ] 从最终 Windows EXE 执行原生用户路径。
- [x] Android ARM64 签名 APK 构建成功，版本 0.5.0、`versionCode=5000`，沿用旧签名；未连接设备，未执行设备运行验证。
- [x] `package.json`、npm lock、Cargo manifest/lock 和 Tauri 配置版本均为 0.5.0；Android manifest 保留 `application/x-textpack`，没有注册通用 ZIP MIME。

## TextPack 打开与往返

- [x] 浏览器打开 UTF-8 中文正文和 PNG 的 `.textpack`，所见即所得与阅读显示图片；切换源码后保留相对路径，未混入 `blob:`。
- [ ] 用完整 JPEG/GIF/WebP 文件验证各视图显示，以及源码预览显示。格式签名识别已有单元覆盖。
- [x] 浏览器编辑、保存、重新打开后正文与 PNG 完整；单元测试保留未引用附件和原元信息。浏览器保存结果为单个 ZIP 字节流。
- [ ] 实际文件系统保存时不生成旁置附件目录。
- [x] 浏览器及真实剪贴板测试对比保存前后 PNG 字节，原字节一致，Markdown 中未写入 Base64。
- [x] 浏览器普通“另存为”输出 `.textpack` 并保留包内图片，使用模拟文件句柄验证。
- [ ] 将落盘文件移动到真实新目录后重新打开。
- [x] 单元测试覆盖 ZIP 根目录、单个 `.textbundle/` 子目录、无效元信息及缺少正文的拒绝。
- [ ] Windows 命令行启动和拖放能打开 `.textpack`，大小写扩展名与中文文件名有效。
- [ ] 最近打开能重新授权并读取 TextPack，恢复对应模式和阅读位置。

## Markdown 转换与插图

- [x] 浏览器点击“更多”→“另存为 TextPack”，通过模拟 Tauri `read_local_image`、保存对话框与二进制写入完成转换；重复图片只读取一次，ZIP 可解包并显示原 PNG。
- [ ] 用原生文件权限和实际磁盘图片完成转换，并移动结果文件验证。
- [x] 单元测试覆盖重复图片、空格/中文路径、编码与转义、内联/引用式/HTML 图片语法；模拟 IPC 集成覆盖内联与引用式共享图片。
- [x] 单元测试确认网络图片保留网址，代码块、行内代码与注释不被错误修改；模拟 IPC 集成保留代码中的图片示例。
- [ ] 缺失图片、无权限图片和不支持的格式转换失败时提示可操作原因，原 Markdown 内容和文件仍保留。
- [x] 浏览器所见即所得模式通过构造的图片剪贴板事件插入截图，源码中出现新附件路径，保存成功；恢复草稿可恢复两张图片。
- [ ] 在各编辑模式用真实截图剪贴板及原生插图选择器完成插入、保存和重开。
- [ ] 新建文档直接另存为 TextPack，再插图保存；确认普通 `.md` 保存和旁置 `assets/` 流程仍正常。
- [ ] TextPack 含有足以触发大文件模式的正文时，打开、切换视图、图片显示、编辑与保存正常。

## 保存、取消与恢复

- [ ] 取消转换或另存对话框，原文档格式、路径、正文、图片与未保存状态保留。
- [x] 浏览器模拟文件写入失败后旧 ZIP 字节不变、未保存标记保留，重试保存成功。
- [ ] Windows/Android 实际磁盘、权限或空间不足时的保存失败恢复。
- [x] 浏览器在添加两张图片并修改正文后，用实际 IndexedDB 生成草稿；刷新后从恢复界面读回正文和图片。单元测试确认结构化克隆保留二进制附件，恢复副本不会修改存储副本。
- [ ] 从恢复后的原生会话继续编辑并保存到实际文件。
- [x] 单元测试覆盖草稿保留 TextPack、成功保存后的删除、写入失败提示，以及“保存并继续”“放弃更改”“取消”的数据保留和异步顺序。

## 网页图文粘贴

- [x] 真实网页选区 Ctrl+C / Ctrl+V 传递 `text/plain` 与 `text/html`，保留标题、段落、加粗和一张 PNG 的顺序。另用构造的 HTML 剪贴板事件覆盖列表、成功图片与失败图片混合的内容。
- [x] 真实剪贴板与浏览器集成均验证 TextPack 内置下载成功的图片，保存并重开后正文和图片完整；PNG 原字节一致。
- [x] 浏览器向空白未保存文档粘贴带内嵌 PNG 的 HTML 后自动采用 TextPack，保存结果中没有 Base64。
- [ ] 单独验证未保存文档仅粘贴文字、图片全部失败时的格式与状态。
- [ ] 已有普通 Markdown 文档在原生版中粘贴网页图文，图片写入旁置 `assets/`，保存后相对路径有效。
- [x] 浏览器路由的 HTTP 404 图片保留可见失败链接及正文；单元测试覆盖请求失败、非 HTTPS、不支持格式、单图超限和存储失败的回退。
- [ ] 使用真实服务器验证 CORS 拒绝与提示。
- [x] 单元测试覆盖每次 32 张、单图 8 MiB 限制与取消。浏览器下载 gate 实测打字、另存为、未命名文档首次保存、切换文档期间取消过期粘贴，当前正文不被覆盖。
- [ ] 专门执行总下载量 32 MiB 边界用例。
- [x] 浏览器“新建 TextPack”后可以粘贴图文与图片，并保存为 `.textpack`；使用模拟文件句柄。
- [ ] 验证原生保存对话框的默认扩展名及用户改名路径。

## 平台差异

- [x] 浏览器打开已有 TextPack 并显示包内图片，模拟文件句柄另存后可重新打开。
- [ ] 浏览器下载回退无法确认落盘时保留原文档状态和未保存标记；不会将下载请求当作确认保存并离开文档。
- [ ] 浏览器转换引用旁边本地图片的普通 `.md` 时，明确提示目录访问限制，原文档保留。
- [ ] Android 系统选择器可选取 `.textpack`，包括 ZIP 或通用二进制 MIME 的提供方；普通 `.zip` 扩展名仍被拒绝。
- [ ] Android 不含扩展名的 `content://` URI 可按文件内容识别 TextPack；包内图片不要求关联图片文件夹。
- [ ] Android 导入首次保存要求选择导出位置；成功导出后当前会话可再次保存该导出文档。
- [ ] Android 转换普通 Markdown 前关联目录，打包后取消依赖旁边的附件目录；取消/撤销权限后给出正确提示。
- [ ] 在 Android 设备上测试文件管理器“打开方式”和分享入口，记录提供方及 MIME；不能用编译成功推定这些入口已通过。

## 格式校验与边界

- [x] 单元测试覆盖损坏 ZIP、CRC 不匹配、路径越界、重复路径、大小写冲突、加密包及不支持的压缩方式拒绝。
- [ ] 增加符号链接与 ZIP64 的专门拒绝用例。
- [x] 单元测试覆盖声明解压大小及总量超过 128 MiB、4,096 条目上限、伪造解压大小和实际解压长度不匹配。
- [ ] 专门执行压缩输入及保存输出 128 MiB 的边界用例。
- [x] 单元测试覆盖 PNG/JPEG/GIF/WebP 格式识别、伪装 SVG 的拒绝与单图 32 MiB 上限。
- [ ] 不将任意 ZIP 或 `.textbundle` 文件夹当作受支持的文档入口；错误打开不破坏原来的编辑内容。
- [x] 单元测试覆盖普通 UTF-8 Markdown 的 BOM、换行与草稿回归；浏览器竞态测试覆盖普通 Markdown 手动保存及再次保存，使用模拟文件句柄。

## 结果记录

| 日期 | 平台 / 版本 | 项目 | 实际结果与范围 |
| --- | --- | --- | --- |
| 2026-09-08 | 最终 v0.5.0 工作树 | `npm test` | 229/229 通过；包含纯逻辑、模拟 DOM/文件 API 等自动化用例。 |
| 2026-09-08 | Windows / v0.5.0 | `cargo test --manifest-path src-tauri/Cargo.toml` | 11/11 通过；覆盖原生函数与临时文件操作，原生界面仍未实测。 |
| 2026-09-08 | 最终 v0.5.0 工作树 | `npm run build:renderer` | 生产前端构建成功。 |
| 2026-09-08 | Windows / v0.5.0 | 最终 EXE | 构建成功，6,629,888 bytes；尚未从该产物执行原生 UI 验收。 |
| 2026-09-08 | Android ARM64 / v0.5.0 | 最终签名 APK | `versionCode=5000`，沿用旧签名；无设备，未验证安装、选择器、分享与真实权限流程。 |
| 2026-09-08 | 隔离 headless Edge | `node scripts/qa-textpack-browser.mjs` | 13 个浏览器流程通过；模拟文件句柄和构造的剪贴板事件，真实编辑器、图片渲染及 IndexedDB 草稿恢复。 |
| 2026-09-08 | 隔离 headless Edge | `node scripts/qa-web-clipboard.mjs` | 3 项通过；两个页面间真实选区 Ctrl+C / Ctrl+V，实际 `text/html`，模拟文件句柄保存/重开，测试路由提供 HTTPS PNG。 |
| 2026-09-08 | 隔离 headless Edge | `node scripts/qa-paste-races.mjs` | 5 项通过；下载 gate 下验证打字/另存/首次保存/切换文档，另通过模拟 Tauri IPC 验证 Markdown 图片打包与二进制保存。 |

三个浏览器脚本均使用独立 Vite 服务与 headless Edge，最终运行无 `pageerror`；主浏览器脚本包含一次预期的注入保存失败弹窗，真实剪贴板和竞态脚本无弹窗。未勾选的原生、设备及专门边界项目继续保留，构建结果与模拟 API 测试各按上表范围记录。
